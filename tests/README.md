# Cross-component tests

Cross-component and lab test matrices will live here. Version 0 contract tests
are colocated with `shared/`, `controller/`, and `dashboard/`; no lab tests or
endpoint execution tests exist yet.

