# Kernel lab

This directory is reserved for detection-only driver inventory and externally
verified, authorised lab evidence. Version 0 contains no drivers, kernel code,
BYOVD implementation, or security-control modification.

