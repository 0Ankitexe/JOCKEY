# Local development values only. Copy this file to .env and replace the
# password before sharing a development host. Never use these values in a
# production or externally reachable deployment.
POSTGRES_DB=jocky
POSTGRES_USER=jocky
POSTGRES_PASSWORD=replace-with-a-local-development-password
POSTGRES_PORT=5432
REDIS_PORT=6379
CONTROLLER_PORT=8000
JOCKY_ALLOWED_ORIGINS=http://localhost:5173

