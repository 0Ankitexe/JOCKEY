module nested_types
target windows
fn collect(items: list<option<endpoint>>, prior: option<result<report, failure>>) -> result<list<report>, failure> {
    return assemble(items, prior)
}
run collect on selected_endpoints
