module bounded_iteration
target windows | ubuntu
fn inspect(items: list<endpoint>) -> report {
    for item in items {
        forensic.process.list(item)
    }
    return report()
}
run inspect on selected_endpoints
