module demo
target windows | ubuntu
fn bad() -> forensic_result { return forensic.system.profile(missing) }
fn main(target: endpoint) -> forensic_result {
    return forensic.system.profile(target)
}
run bad on selected_endpoints
